Setup (mandatory):
	- install pip if not already installed 
	- cmd: pip install requests
	- cmd: pip install decorator
	
Optional:
	- cmd: pip install --user git+https://github.com/kuuurt/pyan.git
	- install dependency for pyon: http://www.graphviz.org/download/
	- example: pyan3 '/d/Programming puzzle pieces/webPage/generator/modules/htmlAttributes.py' --uses --no-defines --colored --grouped --annotated --svg > attr.svg